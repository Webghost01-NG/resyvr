{
  "name": "resyvr",
  "version": "0.1.0",
  "private": true,
  "description": "Proof-bounded multi-issuer asset issuance on Creditcoin",
  "license": "MIT",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "check": "npm run typecheck && npm test && npm run preflight",
    "preflight": "node --dns-result-order=ipv4first --import tsx scripts/preflight.ts",
    "test": "tsx --test test/**/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@gluwa/asc-contracts": "0.2.1",
    "@gluwa/usc-sdk": "0.18.0",
    "dotenv": "17.4.2",
    "ethers": "6.17.0"
  },
  "devDependencies": {
    "@types/node": "26.1.0",
    "tsx": "4.22.4",
    "typescript": "6.0.2"
  }
}
